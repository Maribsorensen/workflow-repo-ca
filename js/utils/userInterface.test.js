import { describe } from "vitest";

describe("Path functions", () => {
  describe("returns true when current path matches href exactly", () => {});
  describe("returns true for root path", () => {});
  describe("returns true when current path includes the href", () => {});
  describe("returns false when paths don't match", () => {});
});
